---
title: Radio Group
description: Displays a group of radio buttons.
---


## Installation

<ComponentInstallationDoc name="radio-group" />


## Usage

```tsx
import { RadioGroup } from '@/components/ui/radio-group';
import { Text } from '@/components/ui/text';

const languages = ['JavaScript', 'Rust', 'Python'];

export function RadioGroupDemo() {
  return (
    <RadioGroup defaultValue="JavaScript">
      {languages.map(language => (
        <RadioGroup.Item key={language} value={language}>
          <RadioGroup.ItemIndicator />
          <Text>{language}</Text>
        </RadioGroup.Item>
      ))}
    </RadioGroup>
  );
}
```